import { FiltertodoPipe } from './filtertodo.pipe';

describe('FiltertodoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltertodoPipe();
    expect(pipe).toBeTruthy();
  });
});
